NAME:插件管理器 V121
BY:IDlike
@JS-START
setTimeout(function (){
    const settingwindow = document.getElementById("settingwindow");
    const plugin_lst = document.createElement("div")
    settingwindow.appendChild(plugin_lst)
    for (let i = 0; i < plugin_list.length; i++) {
        const ap = document.createElement("t")
        ap.innerHTML = "插件: " + plugin_list[i]
        plugin_lst.appendChild(ap)
        const ab = document.createElement("br")
        plugin_lst.appendChild(ab)
    }
},100)
var plugin_mgr_version = 121
@JS-END